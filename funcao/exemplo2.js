const numeros = [1, 2, 3, 4, 5, 6];

// INCREMENTANDO OS VALORES

function total(){

    let soma = 0;
    for(let index = data.length -1; index >= 0; index -= 1){
        soma += data[index];
        console.log(soma)
    }
    console.log();
    return soma;
}
console.log(total());