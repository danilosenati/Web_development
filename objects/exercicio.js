const menu ={
    guaraná: {
        marca: 'Dolly',
        quantidade: 2,
        preço: 9.50
    },
    pizza: {
        sabor: 'Da casa',
        tamanho: 'Familia',
        preço: 25.00
    },
    delivery: {
        entregador: 'Relânpago Marquinhos',
    }
}

const customerInfo = (fullMenu) => {
    // Adicione abaixo as informações necessarias
}

console.log(customerInfo(menu));

const orderModifier = (fullMenu) => {
    // Adicione abaixo as informações necessarias
}

console.log(customerInfo(menu));
