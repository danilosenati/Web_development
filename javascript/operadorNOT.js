console.log((2 + 2) === 4); // POSSUEM OS MESMOS VALORES E TIPO.

console.log(!(2 + 2) === 4); // A NEGAÇAO DO RESULTADO INFORMADO A CIMA.

/**
 * Estamos diante de um operador muito poderoso. Ele pode inverter o valor booleano de
um elemento. Isso mesmo! Se tivermos uma variável ou um valor considerado true,
podemos gerar o resultado oposto simplesmente fazendo '!'variável, ou seja, false.
 */